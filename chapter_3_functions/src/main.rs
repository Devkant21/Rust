fn main() {
    println!("Hello, world!");

    // always call the new functions inside the main function; otherwise it won't execute

    first_fn();
}

// simple function

fn first_fn() {
    println!("first_fn");
}